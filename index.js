/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import PictureList from './src/PictureLIst';

AppRegistry.registerComponent(appName, () => PictureList);
